
/**
 * 大于比较
 * @param {*} pre 
 * @param {*} next 
 * @returns 
 */
export function lessThan(pre, next) {
    return pre > next;
}

/**
 * 小于比较
 * @param {*} pre 
 * @param {*} next 
 * @returns 
 */
export function greaterThan(pre, next) {
    return pre < next;
}